"""Compositional image retrieval pipeline extracted from the project notebook.

The notebook (`compositional-image-retrivial.ipynb`) remains the self-contained
source of truth; this package is a parallel reorganization of the same pipeline
into discrete modules so the code can be reused outside Colab.
"""
